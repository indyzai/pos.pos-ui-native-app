{
  "name": "openpos-mcp",
  "description": "Stdio MCP server for OpenPOS local and self-hosted Cloud task access.",
  "version": "1.1.7",
  "license": "AGPL-3.0-only",
  "type": "module",
  "mcpName": "io.github.indyzai/openpos",
  "main": "dist/index.js",
  "bin": {
    "openpos-mcp": "dist/cli.js"
  },
  "files": [
    "dist/index.js",
    "dist/cli.js",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/indyzai/OpenPOS.git",
    "directory": "apps/mcp-server"
  },
  "bugs": {
    "url": "https://github.com/indyzai/OpenPOS/issues"
  },
  "homepage": "https://docs.openpos.app/power-users/mcp",
  "keywords": [
    "mcp",
    "model-context-protocol",
    "openpos",
    "gtd",
    "tasks"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "bun src/cli.ts",
    "build": "bun build src/index.ts --target node --format esm --outfile dist/index.js --define process.env.NODE_ENV='\"production\"' --external=better-sqlite3 --external=bun:sqlite && cp src/cli.ts dist/cli.js && chmod +x dist/cli.js",
    "lint": "eslint -c eslint.config.mjs . --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "start": "node dist/cli.js",
    "test": "bun test",
    "test:coverage": "bun test --coverage --coverage-reporter=lcov"
  },
  "dependencies": {
    "better-sqlite3": "^12.0.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.24.1"
  }
}