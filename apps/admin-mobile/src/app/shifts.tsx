import { Clock } from 'lucide-react-native';
import { ModulePlaceholder } from '../shared/components/layout/ModulePlaceholder';

export default function ShiftsRoute() {
    return <ModulePlaceholder title="Shifts" icon={Clock} />;
}
